# namespace :woi do
#   desc "TODO"
#   task :remove_hover => :environment do

#   	# file = new 
#   	path = Rails.root.join('public', 'assets').to_s
#   	dir = Dir[path+ "/*"]

#   	dir.each do |filename|
#   		# if File.fnmatch("**application-**.css",filename)
#   		if File.fnmatch("**ie**.css",filename)
#   			puts filename
#   			# f = 
#   			File.open(path+"/testfile.txt", 'w') do |out|
# 			  out << File.open(filename).read.gsub(/[,|}]?([a-zA-Z\-\. _]*:after)/, "<appId>#{replace}</appId>")
# 			end
#   	# 		File.open(filename, "a+") do |v|
#   	# 			v << "test test"
# 			# v.each_line { |line|
# 			#   # line = line + ".Test"
# 			#   # f.puts line
# 			#   line << "and again ...\n"
# 			# }
# 			# f.close
#   			end
#   		end
#   	end

#   end

# end
